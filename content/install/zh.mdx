# 安装 OpenClaw

## 环境要求
- Node.js 20.x
- pnpm 10.12.4

## 快速开始
```bash
pnpm install
pnpm dev
```

## 验证
打开 `http://localhost:3000` 确认首页加载成功。

## 常见问题
- 如果没有 `pnpm`，请先全局安装。
- 如果 3000 端口被占用，使用 `pnpm dev -- --port 3001`。
